2017-11-8   lijin

1：删除读取配置文件类。

原因：配置文件类本身也需要指定读取配置文件本身的位置。还不如直接指定加载库的相对路径。

2：尚未解决的问题：

1）没有对外提供扣款等9个API接口。需要接口函数定义。

2017-11-7    lijin

1：重新编写windows与linux兼容版本智能框架。

原因：
1）首先：目前车道环境中，必须兼容2000-win7，qt版本目前还不确定是否兼容2000.
2）其次：qt版本中的core库太大，大于5M，作为网络传输时，更新较慢，必须为小于1M的动态库。


